import { DictionaryKeyTextPipe } from './dictionary-key-text.pipe';

describe('DictionaryKeyTextPipe', () => {
  it('create an instance', () => {
    const pipe = new DictionaryKeyTextPipe();
    expect(pipe).toBeTruthy();
  });
});
